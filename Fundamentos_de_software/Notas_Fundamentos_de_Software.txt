https://platzi.com/clases/ingenieria/
Señal del teclado->Motherboard->CPU>O.S. Windows()->
Google Chrome->Evento->Javascript->AJAX->JSON->REST->HTTP->
https://google.com/enviarMail (protocolo, dominio(dns), instrucción)->
dns->servidor->linux->ethernet->tcp/ip->http->apache->java o php->base de datos->
stmp/pop->dns/ip->email->server (postfix)->bandeja->notificación de email.
servidor de email->
Bits y Bytes.
IBM define en 1956 definió el ASCII y el Byte como estándar para los nuevos cpu.
El estándar utf-8 puede usar desde 1 byte hasta 4 dependiendo del simbolo a representar.
El estándar utf-16 puede usar desde 2 bytes hasta 4 dejando más espacios libres para representar hasta emojis.
UNICODE es la empresa que define los estandares de codificación.
Las cpu se pueden comparar mediante hz, Cores y utroas propiedades.
Las cpu están hechas de silicio.
La Bios es un SO de arranque detectando todo lo que está conectado y le da la señal de inicio a la cpu.
El disco duro contiene el Sistema Operativo y se conecta al cpu cuando la BIOS lo detecta.
Para comunicarse el disco duro con la cpu usa la RAM como bus para comunicarse.
Un Memristor lográ guardar la onda eléctrica aun desconectada, siendo igual de veloz que la memoria RAM.
La GPU se comunica entre la pantalla y la cpu y se especializa en hacer muchos procesos en paralelo.
Las GPU pueden estar optimizadas para matematicas tridimensionales y algebra vectorial mas rapido, esto permite mejor velocidad en render y videojuegos.
SoC System on a chip conjunto de varios elementos(cpu, ram, dsp, gpu) en un solo chip.
En un sistema movil hay un controlador de bateria para saber su estado y cuando suministra energía.
Los discos duros antiguos funcionan como los vinilos antiguos: tienen un cabezal que va leyendo los datos que pasan por debajo de ella a medida que el disco gira a una velocidad constante.
Un disco duro es persistente, secuencial y guarda los archivos estructuradamente.
Sistemas de archivos en linux: Ext3, ext4 Windows: FAT(File Alocation Table), NTFS (New Technology File System) Mac: HFS(Herarchical File System), APFS (Apple File System).
En la cabecera del disco duro o indice los archivos es donde están todas las direcciones de los archivos.
Un software de recuperación de datos inspecciona todo el disco duro buscando un patrón conocido para asi recuperar las localidades del archivo.
La memoria cache es una memoria aun mas veloz que la RAM donde se ejecutan los procesos principales del SO.
Una parte del sistema Operativo se guarda en cache, otra en la ram y lo demas en el disco duro.
La CPU y la RAM tienen un indice compartido por el cual accede a los datos de una manera mas veloz.
El bus de datos de un disco duro es sata o antiguanmente IDE.
El puerto PCI Express comunica la GPU con con el CPU.
La GPU tiene muchos nucleos y una velocidad de reloj menor a la de un CPU.
La GPU puede realizar operaciones 3d, codecs y otros algoritmos.
La GPU tienen muchos módulos para realizar en paralelo los calculos fisicos, codificaciones de audio, el render entre otros.
Los sistemas operativos cuando se cargan se cargan en un formato de anillo, los SO tienen un nucleo o Kernel lo cual se carga en el inicio en la memoria RAM.
El Kernel tiene acceso a todo el sistema.
Entre mas lejos estemos del nivel del Kernel menos permisos se tienen (Kernel, Drivers, Drivers Apps).
Para que google tenga acceso al microfono del SO le pide permisos a cada capa hasta llegar a la del kernel donde se verifica la correcta identidad, de ahi le puede dar un permiso momentaneo o por todo el tiempo necesario.
La estandarización en la computación facilta mucho la interoperabilidad en distintos sistemas.
Internet no funciona con satélites.
Internet no es a prueba de armas nucleares.
Un switch comparte la red entre varios equipos PC1, Lap1, PC2.
Un switch rebota la señal hasta encontrar cual es el correcto destinario.
Router wifi emite una señal que permite la conexión inalambrica entre miembros de una misma red.
El router wifi necesita saber el nombre de la red, el password y la lista de Ip.
DHCP (Protocolo para decidir a que equipo entregar la información).
Todos los dispositvos con conexión a internet tienen un hardware con una MAC address para poderse identificar.
La MAC Adress es un número unico.
Modem del ISP(Internet service provider) es el elemento que provee de internet a los demás, se puede conectar por ADSL, Teléfono, 4G/LTE, Fibra óptica.
En una red local Ipv4 no cambia mas que el ultimo byte. El numero asignado depende de quien llego primero a la lista del router.
La ip por default o localhost es la 127.0.0.1.
IPLan: Ip del área local.
Los router distribuyen una lista de ip en la LAN.
Si hay mas de 255 dispositvos entonces se necesitan subredes.
Si en una red local hay mas de 255 dispositvos se tiene que crear una subred.
Para calcular el número de ip se consideran 255*255 ya que la terminación 255 está reservada para Broadcast y el ultimo número tampoco puede ser 0.
En los esquemas de red se crea una red virtual dentro del SO al que se le llama puerto.
Hay 65,535=2^16 puertos distintos.
El SO distribuye los puertos atraves de anillos como los del kernel.
Los puertos del 1-1024 son puertos reservados para ejecutarse atraves del administrador.
Hay muchos protocolos que corren sobre los puertos el mas popular es el http y su puerto es el 80.
Un computador corriendo por ejemplo BitTorrent usa multiples puertos (6881-6889).
El protocolo https usa el puerto 443.
Otro protocolo es el SSH (Secure Shell) este funciona en el puerto 22.
La máscara de red nos indica cuales son los bits de la ip que no pueden cambiar dentro de una red local.
A lo largo de internet hay muchas ip por lo cual se necesita una ip global y una ip local.
Para que existan las ip globales y locales un grupo de ingenieros creó un documento (RFC), un rango de ips que nunca estaran disponibles en el internet público sino solo en el internet privado (del 192-255).
Hay esquemas de red solo disponibles para redes locales, por lo general la máscara de red es 255.255.255.0.
El Gateway comunmente 192.168.10.1, es hacia donde se dirige la información.
El gateway hace parte del router wifi aunque no necesariamente del modem.
El router se conecta con el modem a través del protocolo NAT.
El router es el que tiene el Gateway Ip.
NAT( Network Adress Translation) protocolo que traduce de las direcciones de internet a las direcciones de la red local.
IP es la sigla de Internet Protocol y una dirección IP es un número único con el cual una computadora o un dispositivo se identifica cuando está conectada a una red con el protocolo IP.
Internet funciona con satelites en menos del 1% de tráfico, la mayoria es cableado.
Los ISP(provedores de internet) los que nos mandan un cable con la señal a nuestras casas y a su vez ellos se conectan a internet.
London Internet Exchange (LINX) es un punto de acceso para poder intercambiar datos por internet.
Internet Exchange Point es una infraestructura física a través de la cual los proveedores de servicios de Internet intercambian el tráfico de Internet entre sus redes.
Los barcos y casos especiales si se conectan por satélite, pero en la mayoria de los casos todos se conectan por cables que interconectan distintos lugares a través del mundo siendo algunos de ellos cables submarinos.
La concentración mayor de estos cables estan en europa, los cuales superan a los de toda latinoamerica a pesar de ser esta mas grande en extensión.
Hay cables que no estan interconectados con la red pública sino en una red privada como la de china.
Los primeros cables submarinos fueron de cobre para transmitir señales entre los telégrafos de europa y estados unidos.
Los cables actuales son de muchos alambres de fibra optica recubiertos y que pasan por lo mas profundo del mar para que asi esten mas seguros.
Los cables tienen mantenimiento por empresas dedicadas a esto y se enfrentan a diversas situaciones.
Los DNS (Domain Name Server) son servidores con una base de datos, con la cual saben a que IP pertenece un nombre.
Antes de abrir un sitio web primero se debe hacer una petición al DNS.
twiter.com=104.244.42.65
Un dominio puede tener subdominios, como ejemplo: su versión móvil, la api entre otros, estos subdominios pueden tener la misma ip o una distinta.
Los DNS guardan el servidor especial de envio de emails.
Los emails tienen un protocolo único.
MX Records = IP Server Gmail.
Los DNS también guardan Records txt, donde solo se almacena texto, al hacerle la consulta de este texto no necesita dar una ip pues ya tiene el texto guardado en el DNS.
En la computadora se guarda una copia local del DNS con las Ip de los sitios visitados.
Las conexiones cableadas en internet son muy caras, y el tráfico de datos es compartido entre todos, siendo de esta manera caro conectarse a los internet exchange points (red cableada mundialmente).
La ultima milla es la conexión entre el modem y el isp.
Si hay una petición de datos entre 2 dispositivos con el mismo isp, lo que habitualmente pasa es transferir los datos a través de una MAN (Metropolitan Area Network), sin pasar por el IXP (Internet Exchange Point), de esta manera el ISP no paga por el uso del IXP.
Usando este principio los ISP pueden ofrecer servicios locales de alta velocidad de transferencia.
Cuando es necesario conectarse por IXP, los servidores crean prioridad, angostando la conexión a través de routers ,firewalls y network shapers, para asignar la prioridad a emails, llamadas por internet o acciones empresariales (de donde mas fondos obtienen) y bajan la prioridad a videos o descargas de archivos secundarios como los de bitorrent.
Quality of Service (también llamado Network Shaping o Throttling) sirve para regular que sitios tienen preferencia a una mayor velocidad y cuales no, esto se hace en la ultima milla.
Los dueños de los dominios en un intento para contrarrestar el efecto del Throttling, implementaron un concepto llamado CDN ( Content Delivery Networks), con lo cual basicamente hacen una copia de los archivos que son estáticos en distintas partes del mundo para asi tener mas cerca de cada usuario la información y esta pueda viajar sin problemas.
La conexión de los servidores de internet se mide en Megabits Mb, pero la información viene en bytes, por ende son velocidades mas lentas de lo que esperaríamos en tiempo de descarga.
Con una conexión de 10Mb/s solo se pueden transmitir 1.25 MB/s.
Además la velocidad de red se ve reflejada por cuanto tiempo tarda la conexión en establecerse y pues fisícamente tiene que viajar de un lugar a otro y para ello se usa el ping que es el tiempo que tarda la conexión en establecerse.
ping www.platzi.com Es lo que tarda un bit en llegar desde la pc hasta el servidor.
La velocidad del ping se ve limitada por la distancia que tiene que recorrer, por ejemplo un barco manda la señal al satelite luego al IXP y de ahi se dirige al servidor.
El tiempo de un ping es muy importante para videojuegos en tiempo real o videos.
Una señal que viaja de Mountain view a Madrid demora 9344Km/(300Km/ms)=31.14ms, siendo esto el tiempo mínimo para transferirse, para aumentar su velocidad si son muchos datos se pueden enviar en grandes paquetes en paralelo para que lleguen mas datos a la vez.
Lo que en verdad venden los ISP es el ancho de banda que puede transmitir al mismo tiempo.
El cliente esta conformado por el navegador desde el cual se invoca un archivo Javascript para mostrar distintas cosas en el navegador, sin embargo también se pueden hacer peticiones de información al servidor remoto.
Esto se divide en Backend y Frontend.
El lenguaje mas popular de backend es php, sin embargo existen otros como python, node js, java.
Los servidores mas populares son programas que procesan el código del backend, como nginix, apache, internet information service y node JS.
Un stack es un grupo de tecnologías, uno de estos es LAMP.
El ciclo de un stack es asi:
El navegador le pasa la url al servidor DNS (DIN DNS),el servidor DNS cambia el domino de platzi por una IP,luego el servidor de platzi recibe la url con apache o nginix, luego los datos son pasados al backend, en este caso go, python y node js, estos procesan la información para hacer una petición a la base de datos, obtienen los datos y mandan de respuesta una página con html, css y js, el cual le mandan al navegador.
El código del frontend siempre es visible, por lo cual este le hace peticiones al backend el cual se encargar de obtener la información y a la vez protegerla.
Existen muchos metódos para conectarse desde el el frontend (html, css y Javascript) como el get, post, ajax, sockets.
Get manda los datos en la url, Post los manda a través de los headers http.
Para hacer cambios sin recargar la página ya sea por usar post o get se puede usar AJAX, el cual en el mismo javascript hace una petición asíncrona y modifica una cierta parte del html o css.
Un stack popular es MEAN el cual está formado por mongoDB, express, AngularJS (libreria para frontend) y Node-JS.
Noje JS es muy efectivo para sockects transfiriendo información entre cliente servidor en tiempo real.
Los videojuegos y los chats usan sockets para una transmisión mas rápida.
Pasos para acceder a un sitio web www.platzi.com/fundamentos.
El sitio web ve si tiene la ip en cache, sino la consulta del dns.
El navegador forma un http request usando el verbo GET.
En el mensaje manda [GET /fundamentos HHTP/2, Host:www.platzi.com, User-Agent: Chrome 28]
Se le pasa el host ya que una dirección ip puede tener multíples dominios.
Los datos en el mensaje GET son enviados a la IP del servidor en el puerto 80(http) y el servidor recibe la petición.
El servidor responde la petición (http response) en un formato muy parecido al request.
[HTTP 200OK //Estado de la respuesta.
Date: xxxx
Server: nginx 18 (Linux) //Servidor en que corre la aplicación.
Last-Modified: xxxx //Permite saber si no hay modificaciones.
Content-Length:13 //Cantidad de bytes que tiene la petición.
Conecction: close //Especifíca si va a mantener la conexión cerrada o abierta (chats por sockets).
Content-type: text/html //Tipo de dato que se envía.
<h1>hola</h1>]//Manda el código HTML.
Si se vuelven a hacer peticiones desde el sitio como usar AJAX (Assets) o nuevas peticiones(Resquest) se vuelve a repetir el proceso pero usando los links de la página web.
Las cookies siempre se envian por medio del request, se vayan a usar o no, luego el servidor actualiza la cookie y la manda de regreso.
La cantidad de cookies en una aplicación hacen mas pesado tanto el envío como la respuesta ya que siempre van en ambas.
1.- Los protocolos se encargan de manejar todas las peticiones que hacen la páginas de internet desde tu navegador hacia los servidores DNS, éstos transforman la dirección de la página web en una dirección IP y tu navegador se conecta a esa IP.
2.- Una vez se tiene la dirección IP el navegador envía un HTTP request en donde envía información con las características del cliente y los requerimientos del mismo, es decir, Host requerido, página del sitio que necesita, tipo de navegador, versión del navegador, etc.
3.- El servidor envía los resultados por medio del mismo protocolo HTTP en forma de un HTTP Response en donde manda todo el HTML del sitio web así como otros datos que el navegador necesita.
4.- Por último se cargan los assets de nuestro sitio web y es aquí donde se descargan imágenes, sonidos, etc.+
Por lo general se usa TCP/IP como protocolo de transmisión pero también se puede usar UDP siendo mas rapido pero mas errático.
Hay varios tipos de wifi con estándares distintos y cifrados distintos.
Se pueden adicionalmente hacer conexiones con sockets en vez de usar http, siendo estas útiles para conexiones persistentes que nunca se caen.
Para detener las conexiones y permitirles el acceso se usa un firewall.
La dark web también denyro de ella TOR, siendo una forma compleja y cifrada de conectarse a internet en la que no se tiene registro de quien accedió.
Para poderse conectar a internet desde el celular se usa una tecnología llamada Tethering (con un puerto usb o bluetooth puede darnos internet).
Cuando no se quiere usar Internet Exchange Points se debe usar Peer to Peer.
Con las redes Mesh se puede tener un internet mas robusto que no dependa de cables submarinos.
Con un VPN se puede tener mas privacidad en la ip.
La mayoria de las ip son asignandas dinámicamente pero también hay fijas.
El protocolo http envía la información en paquetes.
Para combinar múltiples redes se puede usar Multi-WAN y Round Robin.
Sync/Ack, Sync Acknowledge (enviar dato y aceptar que el dato fue envíado).
Sistemas Operativos:
-Windows es el sistema operativo de propósito general más usado a nivel mundial, es un sistema operativo cerrado y se encuentra en la gran mayoría de computadoras para consumidores además utiliza un núcleo propietario perteneciente a Microsoft.
-Linux es el sistema operativo más utilizado en servidores, es libre y su creador Linus Torvalds aún sigue desarrollando su núcleo destacado por su alto rendimiento y alta seguridad, tienen una licencia del tipo GNU-GPL que no solo permite redistribuir si no también garantiza que las personas que redistribuyen el código deban aportar a la licencia entre otras cosas.
-FreeBSD es el sistema operativo en el que está basado Mac OS y muchos sistemas embebidos así como firewalls como pfsense y más, tiene una licencia del tipo BSD.
Hay una diferencia entre Open-Source y Libre, donde libre puede modificarse y distribuirse.
GPL es la licencia GNU para distribución de software libre.
Hay códigos que en algunas partes son libres y en otras cerrados, pues hay logos o diseños protegidos por marcas.
La licencia mas popular para distribuir linux es GNU-GPL.
El Kernel es el núcleo del SO.
En el Kernel no está como se representa gráficamente la información, solo los drivers más básicos, ni la capa de aplicaciones y permisos.
El Kernel de Windows es un kernel de Microsoft, han existido versiones del Windows que no cambian de Kernel.
Linux tiene una organización sin lucro llamada Kernel.org siendo el Kernel del So.
En Linux todas las piezas son separadas como la interfaz gráfica, los drivers, etc.
FreeBSD, quiere encargarse de desarrollar todo SO, en vez de que este sea separado por piezas.
MacOS comomenzo usando BSD como Kernel y llegando así a Next, luego Darwin y finalmente XNU el cual es el Kernel de MacOS.
Los SO basados en UNIX son SO inicialmente basados en Consola.
En Windows a partir de Windows 1.0 se tuvo una interfaz gráfica propia, evolucionando asi hasta windows 10 donde se tiene responsive design para distintos dispositivos.
La capa de interfaz gráfica para los SO basados en UNIX es X11, de donde se crean distintos sistemas visuales como KDE y Gnome.
Existen varias distribuciones de Linux como Ubuntu, RedHat, Mandriva, además existen distribuciones orientadas a hacking como Kali Linux.
Existen iOS, tvOS y watchOS como SO de macOS.
Android es un SO basado en el Kernel de Linux, pero modificado para funcionar con sistemas móviles, con drivers para GPS y Telecomunicaciones, todo esto fue hecho por Google.
Además cada Android fue modificado por los distintos fabricantes de teléfonos celulares como Samsung, Sony.
Los SO vienen de una empresa llamada Xerox, ya que está fue la primera que dió un SO visual (Xerox Star), pero no lo consideró relevante.
Los niveles de permisos en los SO están en general dados por Admin (crear y modificar) Team(modificar) y Public(leer).
Todo archivo puede ser ejecutable si se le otorga permiso de execute.
rwx - read write execute. Son instrucciones que otorgan los distintos permisos al archivo.
En linux con chmod comando se le dice que permisos tiene ese archivo.
Si se le otorga un permiso de ejecutar a un archivo no deseado puede generar un hueco de seguridad, por ejemplo en el servidor.
Para cambiar las propiedades de un archivo se deben tener permisos de administrador.
El comando sudo es para acceder como administrador o root.
Permisos, niveles de procesos y privilegios de ejecución
En la administración de archivos la capacidad de utilizar permisos te permite definir entre las siguientes características, los permisos existen en todos los sistemas operativos de diversas formas y se crean con las siguientes opciones:
Read ®: permisos de lectura.
Write (w): permisos de escritura.
Execute (x): permisos de ejecución.
Los sistemas operativos móviles difieren de los normales sobretodo en los permisos especiales que requiere la app para funcionar, al momento de distribuir tu app ya debes tener o saber que permisos vas a usar.
En Android se encuentra la tienda de Google y la de Amazon para aplicaciones entre otras.
Las primeras aplicaciones en los iphones se instalaron furtivamente usando Cydia.
Tanto en Windows, MacOS y Linux se pueden instalar apliciaciones de diversas formas, en Linux se usan los distribuidores de paquetes como apt-get o como yum que se conectan a un repositorio de donde pueden descargar todo lo necesario.
En Windows se puede usar chocolatey o en MacOS HomeBrew como distribuidores de paquetes opcionales a instalar.
Algunos permisos solicitados son el GPS, Accounts, acelerómetros, cámara, micrófono, contactos, galería de fotos, archivos.
Android permite acceder a la tarjeta SD si lo permite, con un sistema de archivos basado en un sistema Linux(*nix).
Los Iphone tienen un sistema de archivos basados en contenedores (sanbox) por lo cual no se podrá acceder a él mas que desde la app que lo hizó. También usa file sharing api, para crear un enlace entre aplicaciones y asi pasarse los archivos. La principal manera de guardar en IOS es mediante icloud almacenando todo el contenido en la nube, pero para mayor almacenamiento se debe pagar.
Para desarrollar nativimente en Android se usa Java (Dalvik), mientras que en IOS se usa Objective C o en Swift.
En ambos se puede usar C#, C++, Unity, Unreal ya que son multiplataforma y se usan en general para videojuegos.
Un entorno para programar en Apple es XCode, mientras que para Android es Android Studio.
En 2014 Google entregó 7 mil millones de dólares a los desarrolladores de Android, mientras que Apple le entregó 10 mil millones de dólares a los desarrolladores de IOS.
En Google también se puede ganar donero por medio de adds, en apple no hay publicidad pero es mas común pagar las apps.
Existen herramientas que se pueden usar para desarrollar aplicaciones que funcionaran para ambos SO pero con un coste en rendimiento.
Xamarin-Se usa c#.
React Native- Se usa JS y React.
Ionic- Angular.
¿Cómo funciona la compresión de un archivo sin perder ningún valor de ese archivo?
Los árboles binarios nos permiten comprimir sin perder información. En este caso, vamos a comprimir “amo leer panama papers”.
a)Debemos ver cuantas veces se repite cada letra.
a = 5,m = 2,r = 2,s = 1,o = 1,  = 3,p = 3,l = 1,e = e,n = 1
b)La letra con más frecuencia va a estar en el primer punto de la rama. Cuando se encuentra es 1, y cuando no se encuentra es cero.
c)Con esto debemos volver a construir nuestro mensaje siguiendo el árbol, esto quedaría:
1 00001 0000001 01 00000001 001 001 000001 01 0001 1 0000000001 1 00001 1 01 0001 1 0001 001 000001 000000001
El algoritmo de los árboles binarios para compresión de archivos está basado en ocupar los caracteres con mas frecuencia en menor cantidad de bytes y los de mayor en mayor cantidad de bytes.
Además se debe guardar el significado de cada byte desde la raiz para poder recuperar el byte original.
Tu aprendiste que .jpg significa la extensión de archivos de imagen, históricamente windows tenia muy pocos bytes para la extensión, por esto la extensión normalmente era de 3 dígitos.
Existen muchas otras extensiones como .html para páginas web, .mpg4 para vídeo.
Cuando abres los archivos vas a ver su codificación binaria o dependiendo del editor puedes verlo en hexadecimal.
Un sistema operativo lee los primeros bytes del archivo para entender a que archivo corresponde, esta información se llama cabecera.
Cada sistema operativo tiene una base de datos de que programa abre que tipo de archivo.
Cuando estas transmitiendo un archivo por Internet se especifica el tipo de archivo con mime types, el cual se transmite en la cabecera de un paquete http.
Los metadatos literalmente «sobre datos», son datos que describen otros datos. En general, un grupo de metadatos se refiere a un grupo de datos que describen el contenido informativo de un objeto al que se denomina recurso.
Al inicio las extensiones (originarias de windows), tenian solo 3 carácteres y 8 bytes para el nombre del archivo. Debido a esto suceden cosas como que un navegador acepte archivos .htm en vez de .html .
Los archivos binarios o que no son de texto almacenan los datos en formato hexadecimal en vez de código ASCII o algún tipo de codificación.
Con la extensión y los primeros datos guardados del archivo los Sistemas operativos pueden saber de que tipo de archivo se trata y si este no tiene errores de formato, ese patron se llama cabecera de archivo.
Al instalar un nuevo programa al SO, este le puede agregar la información de como debe ser la cabecera de un archivo para una determinada extensión.
Cuando se usa un archivo de internet el cual no tiene extensión se usan los MIMES types, en la cual en los metadatos html, se manda el tipo de archivo que se está transmitiendo (en content-type).
MIME: image/jpeg, text/html, video/mp4.
Gracias a los MIMES types el navegador puede interpretar solo una parte del video, imagen o contenido sin tener que leer primero todo el archivo.
Para realizar una compresión de imagen en jpg basicamente se analiza donde hay áreas de color semejante y estas se asignan a un numero determinado con el cual son guardadas.
Pasan por el siguiente proceso:
RGB a YCbCb: Pasan de RGB a luminancia y crominancia.
Submuestreo: Se sacan escalas de mayor tamaño de las crominancias.
DCT: Se aplica una transformada de fourier con base en cosenos.
Cuantización: Se agrupan por rangos los resultados de la transformada.
Huffman: Se eliminan los ultimos datos repetidos.
Aplicando este algoritmo se puede comprimir una imagen en un rango de hasta 120:1.
Para comprimir un video se usan lois frames y como estos van cambiando con el tiempo.
Existen distintas codificaciones de video como .avi, .mp4, .flv, .mpg, .webm
En japon existe un formato llamado mkv que permite aregar subtitulos y estilos a un video permitiendo asi su traducción a varios lenguajes.
Con multipart se envia la cabecera del video para tener siempre la información del tipo de video y que asi se pueda reproducir sin tener la información anterior, esto se puede usar en transmiciones en vivo.
Un codec es un algoritmo de compresión y descompresión de video.
Algunos codecs son divx, h.264, vp9, ogg.
El codec h.264 de mp4 tiene una patente conformada por un consorcio de Apple, Universal, Microsoft, Sony entre otros.
La señal de TV llega codificada en .h264, además en los celulares y diversos dispositivos existen circuitos especializados en decodificar este codec, dejando incompatible con otros codecs de manera optima.
Los circuitos especializados en decodificar son usados entre otros motivos para ahorro de batería pues no le deja los calculos al cpu.
Existen distintos protocolos para transmitir los videos que es la forma en que se transmiten los videos, estos se encargan de poder ver cualquier parte del video en todo momento sin descargar todo el video.
Algunos protocolos son HTTP, RTMP.
Con los KeyFrames se puede asignar los momentos de tiempo en los que hay parecidos en la imagen para no cambiarla de estado y asi usar menos memoria.
Para codificar el sonido se puede usar .mp3 el cual solo deja los datos audibles por el oído humano.
Para guardar una menor cantidad de bytes en el audio se usan los Kbps, que representan los kilobits por segundo que tiene una onda de sonido.
Algunos formatos de sonido son .mp3, .aac, .flac, .ogg y .wav.
En un vídeo hay muchos factores para comprimir, un vídeo es si es una serie de fotos vistas muy rápido. por ejemplo si un vídeo tiene 100 frames a 24 frames/segundo y si cada frame pesara 1.9MB el vídeo pesaría más de 100MB por 4 segundos de vídeo.
Por esto en los vídeos se utilizan varias formas de compresión
Contenedores: es el formato es que se guarda el vídeo como .avi, .mp4, .flv, .mpg, .webm
Codecs: es un algoritmo que comprime un vídeo y lo descomprime como divx, h.264, ogg, vp9
Protocolos: es la forma de transmitir los vídeos como RTMP.
Keyframes: cada cierta cantidad de frame existe un frame que vuelve a definir todo el área.
Una red neural está conformada por nodos de entradas, luego puede pasar por otras capas de procesamiento y finialmente arrojan una salida.
Algunas redes normales son Perceptron, Feed Forward, Radial Basis Network, Deep Feed Forward, Recurrent Neural Network, etc.
Para los algoritmos de vision artificial con redes neuronales, se usan multiples capas de redes neuronales, cada una encargada de detectar un patron en específico, para al final decidir entre todas si es o no lo que se buscaba.
En una base de datos no relacional se tienen documentos o variables, que se parecen entre si, pero no son similares del todo.
Sus ventajas son su velocidad y livianez.
Las bases de datos no relacionales usan en su mayoria JSON para guardar los datos.
Un algoritmo es un conjunto de acciones entrelazadas entre si para tener una funcion de acuerdo a como sea estructurada, es decir un algoritmo puede ser basicámente una forma de resolver una suma simple donde tiene un inicio un proceso y un final.